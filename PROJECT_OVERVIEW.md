# 보틀팩토리 디바이스 앱 - 프로젝트 개요

## 📋 프로젝트 소개

**보틀팩토리 디바이스 앱**은 카페 내 키오스크에서 리턴미컵(다회용 컵) 대여/반납 서비스를 제공하는 웹 기반 디바이스 애플리케이션입니다. 기존 bottleclub 모바일 앱의 키오스크 버전으로, 매장 내 터치스크린 환경에 최적화되어 있습니다.

---

## 🎯 개발 의도

### 핵심 목표
1. **접근성 향상**: 모바일 앱 없이도 매장에서 즉시 서비스 이용 가능
2. **환경 보호 시각화**: 3D 나무 성장으로 사용자의 환경 기여도를 직관적으로 표현
3. **사용자 경험 개선**: 음성 인식, 간편 인증 등으로 편리한 인터랙션 제공
4. **매장 운영 효율화**: 실시간 통계 및 관리자 대시보드로 운영 최적화

### 차별화 포인트
- **3D 보틀 나무**: 개인이 아닌 매장 단위로 성장하는 공동체 나무
- **음성 웨이크워드**: "포장", "테이크아웃" 음성으로 도움말 자동 실행
- **하이브리드 DB**: Firebase(실시간 동기화) + PostgreSQL(통계 분석)

---

## 🛠️ 기술 스택

### Frontend
```
React 19.1.1 + Vite 7.1.12
├─ 라우팅: React Router DOM 7.9.4
├─ 인증: Firebase Authentication (SMS 인증)
├─ 실시간 DB: Firebase Firestore
└─ 음성 인식: Web Speech API + Claude AI (LLM 기반)
```

**주요 기술 선택 이유**
- **React**: 컴포넌트 재사용성, 빠른 개발 속도
- **Vite**: 빠른 빌드 속도, HMR 지원
- **Firebase**: 모바일 앱과 데이터 실시간 동기화

### Backend
```
Node.js + Express
├─ 인증: JWT (JSON Web Tokens)
├─ DB: PostgreSQL on Render
├─ Firebase Admin SDK
└─ REST API
```

**주요 기술 선택 이유**
- **PostgreSQL**: 복잡한 통계 쿼리, 관계형 데이터 관리
- **Firebase Admin**: 서버에서 Firestore 안전하게 제어
- **JWT**: Stateless 인증, 확장성

### 3D Visualization
```
bottler_tree_app (별도 프로젝트)
├─ Next.js + React Three Fiber
├─ Three.js (3D 렌더링)
└─ L-System 알고리즘 (나무 생성)
```

---

## 🎨 주요 구현 기능

### 1. 리턴미컵 대여/반납

#### 대여 플로우
```
전화번호 입력 → SMS 인증 (6자리) → 이용권 확인 → 수량 선택 → 대여 완료
```

**특징**
- 신규 사용자 자동 감지 및 무료 이용권 1개 발급
- Firebase `balances` 컬렉션에서 이용권 상태 관리
- PostgreSQL에 거래 기록 저장 (통계용)

#### 반납 플로우
```
전화번호 입력 → SMS 인증 → 대여 기록 조회 → 수량 선택 → 반납 완료 → 포인트 적립
```

**특징**
- 컵 1개당 30점 적립 (코인 300개)
- 이용권 자동 복원 (재사용 가능)
- 나무 성장 애니메이션 트리거

### 2. 제로웨이스트 실천 기록

사용자가 다양한 친환경 행동을 기록할 수 있는 기능

**실천 항목**
- 텀블러 사용 (30점)
- 다회용기 사용 (30점)
- 리필용기 사용 (30점)
- 자원순환 참여 (5점)

**특징**
- 최대 5개 항목 동시 선택 가능
- PostgreSQL에만 기록 (통계 분석용)

### 3. 3D 보틀 나무 시각화

매장의 환경 기여도를 3D 나무로 시각화

**성장 규칙**
- **가지 수**: 반납 이벤트 횟수에 비례
- **꽃/열매**: 누적 포인트에 따라 해금
  - 30점: 첫 꽃 (분홍색)
  - 100점: 작은 열매 (노란색)
  - 300점: 중간 열매 (주황색)
  - 500점: 큰 열매 (빨간색)
  - 1000점+: 황금 열매

**기술 구현**
- iframe으로 bottler_tree_app 통합
- postMessage API로 실시간 통신
- L-System 알고리즘으로 자연스러운 나무 생성

### 4. 음성 인식 시스템

**방법 1: Picovoice (웨이크워드)**
- "포장", "테이크아웃" 키워드 감지
- 로컬 처리, 프라이버시 보호

**방법 2: LLM 기반 (현재 사용 중)**
- Web Speech API (Whisper) + Claude AI
- 자연어 이해로 다양한 표현 인식
- 5초 세그먼트 단위 분석

**동작**
- 웨이크워드 감지 시 도움말 모달 자동 표시
- 다른 모달 열려있을 때는 무시

### 5. 관리자 대시보드

**기능**
- 전체 가게 통계 실시간 조회
- 거래 내역 Excel 내보내기
- 가게 등록/수정/삭제
- 통계 초기화 (개발용)

**접근 제어**
- JWT 기반 인증
- 가게별 로그인 (가게 ID + PIN)

### 6. 사용자 피드백 시스템

#### 성공 스낵바
- **표시 시간**: 0.8초
- **디자인**: 반투명 배경 (85%) + 블러 효과
- **위치**: 화면 중앙

#### 설문조사 QR 모달
- **표시 타이밍**: 액션 완료 3초 후
- **자동 닫기**: 15초
- **목적**: 사용자 피드백 수집

---

## 🗄️ 데이터베이스 설계

### Firebase Firestore (실시간 데이터)

**주요 컬렉션**
- `users`: 사용자 정보, 포인트, 코인
- `balances`: 대여 이용권 (status: charge/rent)
- `rents`: 대여/반납 기록 (status: rent/return)
- `shops`: 가게 정보, division (individual/group)
- `collect_history`: 반납 이력

### PostgreSQL (통계 데이터)

**주요 테이블**
- `cafes`: 가게 등록 정보
- `transactions`: 거래 기록 (borrow/return/do)
- `user_behaviors`: UI 인터랙션 추적

### 데이터베이스 역할 분담

| 데이터 유형 | 데이터베이스 | 이유 |
|------------|------------|------|
| 사용자 계정 | Firebase | 실시간 동기화, 모바일 앱 호환 |
| 대여/반납 기록 | Firebase | 트랜잭션, 사용자 연결 |
| 통계 집계 | PostgreSQL | 빠른 집계 쿼리, 분석 |
| 거래 로그 | PostgreSQL | 시계열 데이터, 대용량 |

---

## 📱 사용자 인터페이스

### 메인 화면 구성
```
┌────────────────────────────────┐
│  [카페명]          물방울: 150  │
│  "환경을 위한 선택..."          │
├────────────────────────────────┤
│                                 │
│        [3D 보틀 나무]           │
│                                 │
├────────────────────────────────┤
│  [대여]  [반납]  [실천]         │
└────────────────────────────────┘
```

### 반응형 디자인
- **clamp() 함수**: 모든 폰트/간격에 적용
- **vw/vh 단위**: 화면 크기에 따라 유연하게 조정
- **미디어 쿼리**: 450px 이하에서 레이아웃 변경

---

## 🔄 주요 데이터 플로우

### 반납 프로세스 예시
```
1. 사용자 반납 요청
   ↓
2. Firebase 업데이트
   - rents: status 변경
   - balances: 이용권 복원
   - users: 포인트/코인 증가
   ↓
3. PostgreSQL 기록
   - transactions 테이블에 INSERT
   ↓
4. UI 업데이트
   - 성공 스낵바 표시
   - 나무 성장 애니메이션
   - 통계 갱신
   ↓
5. 설문 QR 모달 표시 (3초 후)
```

### Firebase ↔ Mobile App 동기화
- bottleclub 앱에서 QR 대여 → 키오스크 통계 실시간 반영
- 키오스크에서 대여 → 앱에서 반납 가능 (cross-platform)

---

## 🚀 배포 환경

### Frontend
- **플랫폼**: Vercel
- **빌드**: Vite production build
- **환경 변수**: Firebase 설정, API URL

### Backend
- **플랫폼**: Render
- **데이터베이스**: Render PostgreSQL
- **환경 변수**: JWT Secret, Firebase Admin SDK

### 3D Tree App
- **플랫폼**: Vercel
- **프레임워크**: Next.js

---

## 📊 성과 및 특징

### 사용성
- **간편 인증**: SMS 인증으로 앱 설치 불필요
- **빠른 처리**: 대여/반납 각 10초 이내 완료
- **직관적 UI**: 아이콘 + 큰 버튼으로 터치 최적화

### 기술적 특징
- **하이브리드 DB**: Firebase + PostgreSQL 장점 결합
- **실시간 동기화**: 모바일 앱과 양방향 연동
- **확장성**: 마이크로서비스 구조로 확장 용이

### 환경적 가치
- **시각적 보상**: 3D 나무로 기여도 즉시 확인
- **커뮤니티 참여**: 매장 단위 나무로 공동체 의식 강화
- **행동 변화 유도**: 제로웨이스트 실천 기록으로 습관 형성

---

## 🔮 향후 개선 방향

1. **다국어 지원**: 영어, 일본어 등 추가
2. **오프라인 모드**: Service Worker로 네트워크 끊김 대응
3. **게임화 요소**: 배지, 랭킹 시스템 추가
4. **AI 추천**: 사용 패턴 분석 기반 맞춤 서비스
5. **NFC 태그**: 스마트폰 태깅으로 더 빠른 인증

---

**문서 작성일**: 2025-01-29
**버전**: 1.0.0
**프로젝트 경로**: `/bottle_factory_device_app`
